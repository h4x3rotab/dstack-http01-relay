# Example environment configuration for relay-server
# Copy this file to .env and customize the values

# Port to listen on (default: 8081)
# Use 80 for production with sudo, or expose 8081 via nginx
PORT=8081

# Logging level
RUST_LOG=relay_server=info

# Fallback gateway domain when CNAME doesn't match the allowed regex
# Example: dstack-prod5.phala.network
FALLBACK_GATEWAY_DOMAIN=dstack-prod5.phala.network

# Regex pattern to match and extract gateway domain from CNAME
# Default: ^_\.(.+\.phala\.network)$
# The regex should include a capture group to extract the gateway domain
ALLOWED_DOMAIN_REGEX=^_\.(.+\.phala\.network)$

# Which capture group to use as the gateway domain (default: 1)
# Set to 0 to use the entire match, 1 for first capture group, 2 for second, etc.
GATEWAY_DOMAIN_CAPTURE_GROUP=1
